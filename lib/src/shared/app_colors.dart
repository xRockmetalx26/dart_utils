// Dart imports:
import 'dart:ui';

/// Colores del App.
final class AppColors {
  const AppColors._();

  /// Primary
  static const primary = Color(0xFF414141);

  /// `primary.withValues(alpha: .20)`
  static final primaryhighlight = primary.withValues(alpha: .20);

  /// `primary.withValues(alpha: .40)`
  static final primarySplash = primary.withValues(alpha: .40);

  /// Home
  static const greyBackground = Color(0xFF2F2F2F);
}
